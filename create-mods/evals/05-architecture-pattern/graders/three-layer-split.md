---
type: llm
focus: last_message
weight: 1
---
以下の3層構成を正しく説明している:
1. hooks モジュール — イベント登録と $ の呼び出しを一手に引き受ける層
2. surface モジュール — Client 経由でマウントされ、独自の drawing thread を持つ UI 層（モジュールパスが文字列リテラルである必要がある点に触れているとなお良い）
3. pure logic モジュール — $ や UI に依存しない、テスト可能な純粋関数の層

3層を混同したり、1つの巨大なフックにまとめることを推奨していない。
